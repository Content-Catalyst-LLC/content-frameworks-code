# Java Assets

Standard-library Java smoke validation for OKR KPI Measurement Frameworks Catalyst Canvas manifest and generated outputs.

Run from the article directory:

```bash
javac java/MeasurementCanvasValidator.java
java -cp java MeasurementCanvasValidator
```
