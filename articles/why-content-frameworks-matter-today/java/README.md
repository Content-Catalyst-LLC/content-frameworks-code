# Java Assets

Standard-library Java smoke validation for Why Content Frameworks Matter Today Catalyst Canvas manifest and generated outputs.

Run from the article directory:

```bash
javac java/ContentFrameworkValueCanvasValidator.java
java -cp java ContentFrameworkValueCanvasValidator
```
