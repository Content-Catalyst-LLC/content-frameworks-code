# Java Assets

Standard-library Java smoke validation for Ansoff Matrix Catalyst Canvas manifest and generated outputs.

Run from the article directory:

```bash
javac java/AnsoffCanvasValidator.java
java -cp java AnsoffCanvasValidator
```
