# Java Assets

Standard-library Java smoke validation for Message House Catalyst Canvas manifest and generated outputs.

Run from the article directory:

```bash
javac java/MessageHouseCanvasValidator.java
java -cp java MessageHouseCanvasValidator
```
