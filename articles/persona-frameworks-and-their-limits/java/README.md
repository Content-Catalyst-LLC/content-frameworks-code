# Java Assets

Standard-library Java smoke validation for Persona Frameworks Catalyst Canvas manifest and generated outputs.

Run from the article directory:

```bash
javac java/PersonaCanvasValidator.java
java -cp java PersonaCanvasValidator
```
