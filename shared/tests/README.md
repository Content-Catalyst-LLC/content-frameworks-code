# Shared Tests

Future validation tests may check metadata completeness, slug consistency, footer navigation order, internal-link targets, framework category coverage, repository links, and image metadata.
